// LOCAL CLIENT STORAGE HANDLER

const storageHandler = {
    listItem: null, // Placeholder value for the variable
    handleClick: function(event) {
        taskItem = this.newTask // Accesses the current instance of newTask from coreTaskList.js
        taskTitle = this.taskLabel
        
        
    }
}