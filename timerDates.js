// Add new data point
function deadlinePoint(newTaskData) {
    newDeadlineData = {... deadlineDate, ... }
}
// Add coloring scheme
function deadlineStyling() {
    newTaskData.style.borderImageSource = gradient;
    taskElement.style.borderImageSlice = "1";
    taskElement.style.borderStyle = "solid"; // Optional
}